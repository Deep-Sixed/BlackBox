"""Recon — the EVECOR flight recorder.

The session engine (session_record.py) descends verbatim from Project
Snitch: normalized, redacted, append-only session records with evidence
receipts. User-facing naming is Recon (recon_<session>.md, "Recon
Session" headings); `snitch` survives only as a documented legacy alias
for pre-2026-07-16 artifacts and env-var fallbacks.
"""

__version__ = "2.0.0"
